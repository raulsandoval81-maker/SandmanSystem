{
  "name": "sandman-functions",
  "private": true,
  "engines": { "node": "20" },

  "scripts": {
    "build": "tsc --project tsconfig.json",
    "build:watch": "tsc -w",
    "serve": "firebase emulators:start --only functions,firestore,hosting,auth --debug",
    "clean": "rimraf lib && rimraf node_modules"
  },

  "dependencies": {
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^4.7.0",
    "cors": "^2.8.5"
  },

  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^20.19.0",
    "typescript": "^5.9.3",
    "rimraf": "^6.0.1"
  }
}
